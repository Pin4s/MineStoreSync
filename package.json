{
  "name": "@repo/root",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "pnpm": "9.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.6.0"
  }
}
